{
  "name": "ready-to-start-hack",
  "version": "1.0.0",
  "private": true,
  "description": "Hackathon toolkit — pre-configured environment for START Hack Barcelona 2026",
  "scripts": {
    "pitch-deck": "node templates/pitch_deck_template.js",
    "setup:frontend": "cd frontend && npm install",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && source ../.venv/bin/activate && uvicorn main:app --reload --port 8000"
  },
  "dependencies": {
    "pptxgenjs": "^3.12.0"
  }
}
